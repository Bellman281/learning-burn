[package]
name = "c17e2"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.21.0", features = ["ndarray", "dataset"] }
image = { version = "0.25", default-features = false, features = ["png"] }
